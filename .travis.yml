sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t nikhilknick/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nikhilknick/docker-react npm run test
